/**
 * Datei:        ExampleUnitTest.kt
 * Projekt:      Logbot
 * Paket:        de.phytech.logbot
 * Autor:        Phydran6
 * Version:      1.0 – 05.04.2026.15.44.55
 *
 * Beschreibung:
 * Lokaler Unit-Test, der direkt auf der Entwicklungsmaschine läuft (kein Gerät nötig).
 * Dient als Vorlage für eigene Unit-Tests.
 */
package de.phytech.logbot

import org.junit.Test
import org.junit.Assert.*

class ExampleUnitTest {

    /**
     * Beispieltest: Prüft, ob 2 + 2 = 4 ergibt.
     * Kann als Vorlage für eigene Berechnungs-Tests verwendet werden.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
