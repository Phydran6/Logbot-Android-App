<?xml version="1.0" encoding="utf-8"?>
<!-- Alles Seltene liegt im Ueberlaufmenue, damit die Titelleiste leer bleibt. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_biometric"
        android:checkable="true"
        android:title="@string/action_biometric"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_disconnect"
        android:title="@string/action_disconnect"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_about"
        android:title="@string/action_about"
        app:showAsAction="never" />
</menu>
